// [ CompletePage, ErrorPage ] 에서 사용하는 화면 레이아웃
import styled from "styled-components";

export const PageContainer = styled.div`
  height: 100%;
  display: flex;
  flex-direction: column;
  margin: 0 auto;
  gap: 12px;
  justify-content: center;
  text-align: center;
`;
